import React from 'react';


class {{ name }} extends React.Component {
    render() {

        const {store, dispatch, children, page} = this.props;

        return (
            {{ source }}
        );
    }
}

export default {{ name }};
